{
  "metadata": {
    "id": "std.capabilities.map",
    "name": "Capabilities Map",
    "description": "Map of capability keys to capability entries.",
    "fieldType": "composite",
    "dataType": "object",
    "version": "1.0.0",
    "owner": "core.platform",
    "capabilities": ["viewable", "manage"],
    "sensitivity": "internal",
    "constraints": {
      "propertyPattern": "^[a-z]+(?:\\.[a-z0-9_]+)*$"
    },
    "localization": {
      "translatable": false
    },
    "example": {
      "wiki.nav": {
        "enabled": true,
        "version": "1.1.0"
      }
    },
    "provenance": {}
  },
  "$id": "https://eirenhall.com/schema/fields/capabilities.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Capabilities Map",
  "type": "object",
  "propertyNames": {
    "type": "string",
    "pattern": "^[a-z]+(?:\\.[a-z0-9_]+)*$"
  },
  "additionalProperties": {
    "$ref": "./capability_entry.json"
  }
}
